# dotfiles
all of my dotfiles configurations

## VIM

- clean existing vim config for fresh installation
    
    ```plaintext
        rm -rf ~/.config/nvim
        rm -rf ~/.local/share/nvim
        rm -rf ~/.local/state/nvim
    ```
- copy dotfiles to config

    ```plaintext
    cp nvim ~/.config/nvim
    ```